# Superadmin & Mairie Portal Refinement

Implement a superadmin interface for user management and jurisdictional assignment, and fix UI issues in the Mairie portal.

## User Review Required

> [!IMPORTANT]
> The superadmin feature will involve significant changes to user management. I will define a new `AdminUsersPage` for this purpose.

## Proposed Changes

### Database & Backend
#### [MODIFY] [schema/index.ts](file:///Users/evideletang/Desktop/HEUREKA/Web-SaaS-Builder/lib/db/src/schema/index.ts)
- Ensure `usersTable` has a `communes` field (JSON/text) to store assigned cities.
- Add `email` and `password` if not present/restored correctly for management.

#### [NEW] [admin.ts](file:///Users/evideletang/Desktop/HEUREKA/Web-SaaS-Builder/artifacts/api-server/src/routes/admin.ts)
- Create routes for:
  - `GET /admin/users`: List all users.
  - `POST /admin/users/:id/communes`: Update assigned communes for a user.
  - `DELETE /admin/users/:id`: Delete a user.

### Frontend
#### [MODIFY] [portail-mairie.tsx](file:///Users/evideletang/Desktop/HEUREKA/Web-SaaS-Builder/artifacts/heureka/src/pages/portail-mairie.tsx)
- Move the `Commune Selector` to be globally accessible or ensure it's functional within the "IA Config" tab.
- Fix the logic that prevents commune selection in the "IA Config" tab.

#### [NEW] [admin-users.tsx](file:///Users/evideletang/Desktop/HEUREKA/Web-SaaS-Builder/artifacts/heureka/src/pages/admin-users.tsx)
- Create a dashboard for superadmins to manage accounts.

## Verification Plan
- Manual test: Log in as superadmin, assign "Étretat" to a new mairie user.
- Manual test: Log in as that mairie user, verify only "Étretat" is visible.
- UI test: Verify commune selector works in all tabs of the Mairie portal.
